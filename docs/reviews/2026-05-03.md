# Daily Implementation Review — 2026-05-03

## Features Shipped Today
- **ICP / Use-case Onboarding** (commit b32ec85, fixes in ba3e50f)
- **Collections / Stacks** (commit e6b67fc)

---

## Feature 1: ICP / Use-case Onboarding

### Files Changed

**DB (1)**
- `supabase/user_profiles.sql`

**New (5)**
- `lib/onboarding-constants.ts`
- `lib/onboarding-schema.ts`
- `app/api/user/profile/route.ts`
- `app/onboarding/page.tsx`
- `components/OnboardingForm.tsx`

**Modified (3)**
- `app/auth/callback/route.ts`
- `app/profile/page.tsx`
- `types/index.ts`

### Acceptance Criteria

| Criterion | Status | Notes |
|-----------|--------|-------|
| 3-step form capturing role, stage, team, region, budget, use_case | ✅ | |
| Anonymous localStorage draft pre-fills on sign-in | ✅ | key: `currly_anon_profile` |
| Clears localStorage on submit | ✅ | |
| POST/GET API with Zod validation | ✅ | |
| Rate limit on POST | ✅ | 10/min |
| Progress indicator | ✅ | |
| Back button between steps | ✅ | |
| Step-level validation with clear errors | ✅ | |
| `icp_form_viewed` event | ✅ | fires with `is_returning`, `prefilled_from_anon` |
| `icp_form_submitted` event | ✅ | fires with full form + `was_prefilled` |
| `icp_form_validation_error` event | ✅ | fires with `step`, `step_label`, `missing_fields` |
| `icp_form_skipped` event | ❌ | Skip button exists, no event fired |
| Post-login redirect for new users → `/onboarding` | ✅ | auth callback checks `user_profiles` |
| Redirect loop prevention (`next=/onboarding` → `/dashboard`) | ✅ | `finalNext` guard in callback |
| Editable from `/profile` | ✅ | "Edit" link → `/onboarding` |
| ICP chips shown on profile | ✅ | |
| `robots: noindex` on `/onboarding` | ✅ | |

### Edge Cases & Bugs

| Severity | Issue |
|----------|-------|
| Medium | **Skip loop**: users who skip have no profile row → auth callback redirects them to `/onboarding` on every subsequent login |
| Low | `loadedFromAnon` flag is set but no UI feedback shown ("Restored draft" banner missing) |
| Low | `localStorage.getItem(ANON_KEY)` called inside `posthog?.capture()` outside try/catch on line 99 of `OnboardingForm.tsx` — could throw in strict Safari private mode |
| Low | `UserProfile` type uses `string` for all enum fields instead of the typed constants from `onboarding-constants.ts` |

---

## Feature 2: Collections / Stacks

### Files Changed

**DB (2)**
- `supabase/collections.sql` *(updated during migration to add `ADD COLUMN IF NOT EXISTS` guards — original run failed on pre-existing partial table)*
- `supabase/collections_migrate.sql`

**New API Routes (5)**
- `app/api/collections/route.ts`
- `app/api/collections/[id]/route.ts`
- `app/api/collections/[id]/tools/route.ts`
- `app/api/collections/[id]/tools/[toolId]/route.ts`
- `app/api/collections/share/[token]/route.ts`

**Modified API Routes (2)**
- `app/api/user/delete/route.ts`
- `app/api/user/export/route.ts`

**New (10)**
- `components/CollectionPickerPopover.tsx`
- `app/saved/_components/NewCollectionForm.tsx`
- `app/saved/[id]/_components/CollectionManageControls.tsx`
- `app/saved/[id]/_components/CollectionShareControls.tsx`
- `app/saved/[id]/_components/RemoveToolButton.tsx`
- `app/saved/[id]/page.tsx`
- `app/s/[token]/page.tsx`

**Modified (6)**
- `components/SaveButton.tsx`
- `components/DashboardClient.tsx`
- `app/saved/page.tsx`
- `app/dashboard/page.tsx`
- `app/category/[slug]/page.tsx`
- `app/tool/[slug]/page.tsx`

### Acceptance Criteria

| Criterion | Status | Notes |
|-----------|--------|-------|
| New user → no collection → picker → create inline → tool added | ✅ | |
| Existing user → "My Stack" from migration → pre-checked | ✅ | |
| Tool in multiple collections → icon stays blue | ✅ | |
| Remove from all → icon grey | ✅ | |
| Max 50 collections per user enforced | ✅ | |
| Duplicate name returns 409 | ✅ | |
| Delete last collection blocked | ✅ | |
| Generate share link → `/s/[token]` public | ✅ | |
| Revoke share link → 404 | ✅ | |
| Public page indexable (no noindex) | ✅ | |
| `/saved`, `/saved/[id]` noindex | ✅ | |
| Account deletion removes collections (cascade) | ✅ | |
| Data export uses collections not saved_tools | ✅ | |
| Ownership check on all write routes | ✅ | |
| Portal rendering escapes card overflow:hidden | ✅ | createPortal → document.body |
| Popover flips above button near viewport bottom | ✅ | |
| Optimistic toggle with revert on failure | ✅ | |
| Two-step delete confirmation | ✅ | |

### API Validation & Auth

| Route | Auth | Ownership | Input Validation | Rate Limit |
|-------|------|-----------|-----------------|------------|
| GET /api/collections | ✅ | filters by user_id | — | — |
| POST /api/collections | ✅ | — | Zod: name 1–100 | ✅ 20/min |
| PATCH /api/collections/[id] | ✅ | ✅ getOwnedCollection | Zod: all optional | ✅ 20/min |
| DELETE /api/collections/[id] | ✅ | ✅ | last-collection guard | ✅ 20/min |
| POST /api/collections/[id]/tools | ✅ | ✅ | Zod: UUID | ✅ 20/min |
| DELETE /api/collections/[id]/tools/[toolId] | ✅ | ✅ | ❌ toolId not validated | ✅ 20/min |
| GET /api/collections/share/[token] | none | — | UUID regex | — |

### Analytics Events

| Event | Fired From | Status |
|-------|------------|--------|
| `collection_picker_opened` | SaveButton | ✅ |
| `tool_added_to_collection` | CollectionPickerPopover | ✅ |
| `tool_removed_from_collection` | CollectionPickerPopover | ✅ |
| `collection_created_inline` | CollectionPickerPopover | ✅ |
| `collection_created` (from /saved form) | NewCollectionForm | ❌ Missing |
| `collection_share_link_generated` | CollectionShareControls | ❌ Missing |
| `collection_share_link_revoked` | CollectionShareControls | ❌ Missing |
| `collection_deleted` | CollectionManageControls | ❌ Missing |

### Edge Cases & Bugs

| Severity | Issue |
|----------|-------|
| Medium | **`createAndAdd` silent failure**: after creating a collection inline, the tool-add fetch response is never checked. If it fails, UI shows saved but DB isn't updated (`CollectionPickerPopover.tsx:128–131`) |
| Medium | **Ghost-public state**: toggling `is_public: true` without a `share_token` shows a globe icon but the collection is unreachable — both RLS and all routes require `share_token IS NOT NULL` |
| Low | Profile page `savedCount` is from oldest collection only, not total across all collections |
| Low | `handleNewCollection` prepends to picker list (newest first) but DB order is `created_at ASC` — resets on `router.refresh()` |
| Low | `toolId` path param not validated as UUID in DELETE `/collections/[id]/tools/[toolId]` — silently returns 200 on malformed input |

### Migrations

| Check | Status |
|-------|--------|
| `collections.sql` idempotent | ✅ (fixed today with `ADD COLUMN IF NOT EXISTS` guards) |
| `collections_migrate.sql` re-runnable | ✅ `ON CONFLICT DO NOTHING` |
| `saved_tools` preserved for rollback | ✅ |
| `delete/route.ts` still removes `saved_tools` during overlap | ✅ |
| Cascade: deleting collection → auto-deletes collection_tools | ✅ |

---

## Cross-Feature Issues

| Issue | Severity |
|-------|----------|
| Inconsistent API error shapes: ICP uses `{ success, message }`, Collections uses `{ error }` | Low |
| In-memory rate limiter (`lib/rate-limit.ts`) is per-instance — not globally enforced across Vercel function instances | Medium |

---

## Go-Live Checklist

### ICP
- [x] `user_profiles.sql` applied in Supabase
- [x] Auth callback redirects new users to `/onboarding`
- [x] Redirect loop guard active
- [ ] Manual test: new Google sign-up → `/onboarding` → complete → `/dashboard`
- [ ] Manual test: returning user → skips onboarding entirely
- [ ] Manual test: profile page shows ICP chips after completion

### Collections
- [x] `collections.sql` applied (with column guard fix)
- [x] `collections_migrate.sql` applied
- [x] `NEXT_PUBLIC_SITE_URL` set in Vercel
- [ ] Manual test: existing user sees "My Stack" at `/saved`
- [ ] Manual test: save button opens popover with correct pre-checked state
- [ ] Manual test: generate share link → `/s/[token]` works in incognito
- [ ] Manual test: revoke link → 404 in incognito
- [ ] Manual test: delete collection (not last) → gone
- [ ] Manual test: try delete last → error shown
- [ ] Scheduled: drop `saved_tools` table after 48h stability window

---

## Known Risks

| Risk | Impact | Likelihood |
|------|--------|------------|
| Skip-onboarding loop — users redirected to `/onboarding` every login until they complete it | High (activation drop) | Medium |
| `createAndAdd` silent failure — tool appears saved but isn't | Medium (trust) | Low |
| `is_public` ghost state — collection shows Public but unreachable | Medium (UX confusion) | Low |
| In-memory rate limiter not distributed — per Vercel instance | Low at beta scale | Low |
| `saved_tools` table stale in prod — risk of future code touching it | Medium | Low |

---

## Recommended Next Sprint (max 5)

1. **Fix `createAndAdd` response check** — check tool-add fetch response in `CollectionPickerPopover.tsx:128` and show inline error on failure
2. **Fix `is_public` / `share_token` coupling** — disable the toggle when no token exists, or auto-generate token when making public
3. **Handle onboarding skip gracefully** — store a `skipped: true` flag (DB or cookie) so the auth callback stops re-redirecting users who have explicitly skipped
4. **Add missing analytics** — `icp_form_skipped`, `collection_created`, `collection_share_link_generated`, `collection_share_link_revoked`, `collection_deleted`
5. **Drop `saved_tools` + remove `actions/saved.ts`** — after 48h stability confirmation
